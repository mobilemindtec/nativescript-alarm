<Page xmlns="http://schemas.nativescript.org/tns.xsd" loaded="loaded">
  <StackLayout>
    <Label text="Tap to alarm start" class="title"/>
    <Button text="TAP" tap="onTap" />
    <Label text="{{ message }}" class="message" textWrap="true"/>
  </StackLayout>
</Page>